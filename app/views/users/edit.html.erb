<%= link_to("<< Back to List", {:action => 'index'}) %>

<h1>Editing User</h1>

<%= render 'form' %>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>
<% if !@user.admin %>
   | <%= link_to("Make Admin", {:action => "make_admin", :id => @user.id}) %>
<% end %>
