<h2>Search Results</h2>
<br>
<h3> Challenges</h3>
<table>
<!--  <thead>
  <% if not @challenges.blank? %>
    <tr>
      <th>Proposed By</th>
      <th>Title</th>
      <th>Score</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <% end %>-->
  <tbody>
  <% if @challenges.each do |challenge| %>
      <tr>
        <th><%= link_to challenge.name, challenge %></th>
        <td> by <%= challenge.us_chal_interactions.where(:interaction => "created").first.user.username %></td>

  <!--      <td><%= challenge.score %></td>-->

      </tr>
    <% end.empty? %>
      <p><i> There were no Challenges fitting your search. </i></p>
    <% end %>
  </tbody>
</table>
<!--<%= link_to 'Create Challenge', new_challenge_path %>-->

<br>
<div>
<h3> Users</h3>
<table>
<!--  <thead>
  <% if not @users.blank? %>
    <tr>
      <th>Email</th>
      <th>Username</th>
      <th>Points</th>
      <th>Team</th>
      <th>Admin</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <% end %>-->
    <% if @users.each do |user| %>
      <tr>
  <!--      <td><%= user.email %></td>
        <td><%= user.username %></td>
        <td><%= user.points %></td>
        <td><%= user.team_id %></td>
        <td><%= user.admin ? "Yes" : "No" %></td>-->
        <th><%= link_to user.username, user %></th>
      </tr>
    <% end.empty? %>
        <p><i> There were no users fitting your search. </i></p>
    <% end %>
</table>
<!--<%= link_to 'New User', new_user_path %>-->
</div>
<br>
