<%= form_for(@challenge) do |f| %>
  <% if @challenge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@challenge.errors.count, "error") %> prohibited this challenge from being saved:</h2>

      <ul>
      <% @challenge.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    Submitting User
<!--     THIS LINE CURRENTLY ALLOWS DROP DOWN CHOICE OF USER! SHOULD BE AUTO SELECTED TO BE WHOEVER IS LOGGED IN -->
    <%= collection_select(:challenge, :user_id, User.all, :id, :username, {prompt: true}, {:required => true}) %>
  </div>
  <div class="field">
    Challenge Title<br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
